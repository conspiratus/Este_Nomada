# Este Nómada - Новый стек технологий

Проект полностью переписан на современный и безопасный стек.

## 🏗️ Архитектура

### Frontend (Next.js)
- **Next.js 14** (App Router)
- **TypeScript**
- **TailwindCSS** + **Framer Motion**
- **next-intl** (мультиязычность)

### Backend (Django)
- **Django 5.0** + **Django REST Framework**
- **MySQL** (через Django ORM)
- **JWT** авторизация
- **Celery** + **Redis** (фоновые задачи)

## ✅ Что изменилось

### Безопасность
- ✅ Все запросы к БД через Django ORM (без прямых SQL)
- ✅ Валидация данных через сериализаторы
- ✅ JWT авторизация для API
- ✅ CORS настроен правильно

### Удобство разработки
- ✅ Django Admin из коробки
- ✅ Миграции БД через Django
- ✅ Типизированные API через DRF
- ✅ Фоновые задачи через Celery

### Интеграции
- ✅ Telegram синхронизация (Celery задача)
- ✅ OpenAI обработка заказов (Celery задача)
- ✅ Instagram синхронизация (Celery задача)

## 🚀 Быстрый старт

### 1. Backend

```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Настрой .env
cp .env.example .env
# Заполни переменные в .env

# Инициализация
python scripts/init_db.py
python scripts/create_admin.py --username admin --password admin123

# Запуск
python manage.py runserver
```

### 2. Frontend

```bash
# Создай .env.local
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Запуск
npm run dev
```

### 3. Celery (для интеграций)

```bash
cd backend
source venv/bin/activate
celery -A este_nomada worker --loglevel=info
```

## 📁 Структура проекта

```
Este_Nomada/
├── backend/              # Django Backend
│   ├── este_nomada/     # Настройки Django
│   ├── core/            # Модели (Story, MenuItem, Settings, Order)
│   ├── api/             # REST API (ViewSets, Serializers)
│   └── scripts/         # Утилиты
│
├── app/                  # Next.js App Router
│   ├── admin/           # Админка (адаптирована для Django API)
│   └── [locale]/        # Мультиязычные страницы
│
├── components/           # React компоненты (без изменений!)
│   └── sections/       # Секции лендинга
│
└── lib/                 # Утилиты
    └── api.ts           # API клиент для Django
```

## 🔗 API Endpoints

### Публичные (без авторизации)
- `GET /api/stories/` - список историй
- `GET /api/stories/{slug}/` - одна история
- `GET /api/menu/` - меню
- `GET /api/settings/public/` - публичные настройки
- `POST /api/orders/` - создать заказ
- `GET /api/instagram/feed/` - лента Instagram

### Админка (JWT авторизация)
- `POST /api/auth/login/` - вход
- `POST /api/auth/refresh/` - обновить токен
- `GET /api/stories/` - все истории (включая неопубликованные)
- `POST /api/stories/` - создать
- `PUT /api/stories/{id}/` - обновить
- `DELETE /api/stories/{id}/` - удалить
- Аналогично для `/api/menu/` и `/api/settings/`

## 🎨 Визуал

**Визуальная часть не изменилась!** Все компоненты, стили, анимации остались как были.

## 📚 Документация

- `backend/README.md` - документация по Django backend
- `MIGRATION_GUIDE.md` - руководство по миграции
- `DEPLOY.md` - руководство по деплою

## 🔧 Настройка интеграций

### Telegram
1. Создай бота через @BotFather
2. Получи токен
3. Настрой в Django Admin (`/admin/core/settings/`)

### OpenAI
1. Получи API ключ на platform.openai.com
2. Добавь в `.env`: `OPENAI_API_KEY=твой-ключ`

### Instagram
1. Создай приложение в Facebook Developers
2. Получи токен доступа
3. Добавь в `.env`: `INSTAGRAM_ACCESS_TOKEN=твой-токен`

## 🐛 Решение проблем

### Backend не запускается
- Проверь `.env` файл
- Убедись, что БД доступна
- Проверь логи: `backend/logs/django.log`

### Frontend не подключается к API
- Проверь `NEXT_PUBLIC_API_URL` в `.env.local`
- Убедись, что CORS настроен в Django
- Проверь, что backend запущен

### Celery не работает
- Убедись, что Redis запущен
- Проверь `CELERY_BROKER_URL` в `.env`

## 📝 Следующие шаги

1. Настрой переменные окружения
2. Запусти backend и frontend
3. Проверь работу админки
4. Настрой интеграции
5. Деплой на production

---

**Визуал остался прежним, но теперь всё работает безопасно и надёжно!** 🎉



