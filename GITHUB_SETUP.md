# GitHub Integration Setup

## ✅ Что настроено

1. **Git Credential Helper** - настроен для автоматической аутентификации с использованием вашего GitHub токена
2. **Remote Origin** - настроен на `https://github.com/Conspiratus/Este_Nomada.git`
3. **GitHub Actions** - созданы базовые workflows для CI/CD

## 📝 Следующие шаги

### 1. Создайте репозиторий на GitHub

Если репозиторий еще не создан:
1. Перейдите на https://github.com/new
2. Название репозитория: `Este_Nomada` (или другое, если нужно)
3. Выберите приватный или публичный
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)

### 2. Если URL репозитория отличается

Если ваш репозиторий имеет другое имя или находится под другим аккаунтом, обновите remote:

```bash
git remote set-url origin https://github.com/ВАШ_АККАУНТ/ВАШ_РЕПОЗИТОРИЙ.git
```

### 3. Отправьте код в GitHub

```bash
# Добавьте все файлы
git add .

# Сделайте коммит (если еще не сделали)
git commit -m "Initial commit"

# Отправьте в GitHub
git push -u origin master
```

Или если ваша основная ветка называется `main`:

```bash
git push -u origin master:main
```

## 🔐 Безопасность

Ваш GitHub токен сохранен в `~/.git-credentials`. Файл защищен правами доступа (chmod 600).

**Важно:** Токен имеет полный доступ к вашему аккаунту. Храните его в безопасности.

## 🚀 GitHub Actions

Созданы два workflow:

1. **`.github/workflows/ci.yml`** - автоматические проверки при push и pull request:
   - Линтинг фронтенда
   - Сборка Next.js
   - Проверка Django backend

2. **`.github/workflows/deploy.yml`** - деплой на production (требует настройки)

## 🔧 Проверка подключения

После создания репозитория на GitHub, проверьте подключение:

```bash
git ls-remote origin
```

Если все настроено правильно, вы увидите список веток и тегов.

