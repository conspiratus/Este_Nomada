name: Deploy to Production

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:  # Позволяет запускать вручную через GitHub UI

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install frontend dependencies
        run: npm ci
      
      - name: Build frontend
        run: npm run build
        env:
          NODE_ENV: production
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: './backend/requirements.txt'
      
      - name: Install Python dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r requirements.txt
      
      - name: Check Django
        working-directory: ./backend
        run: python manage.py check
        env:
          DJANGO_SETTINGS_MODULE: este_nomada.settings
          USE_SQLITE: 'True'  # Для CI используем SQLite
      
      - name: Create deployment archive
        run: |
          tar -czf deploy.tar.gz \
            .next \
            public \
            package.json \
            package-lock.json \
            next.config.mjs \
            server.js \
            tsconfig.json \
            middleware.ts \
            i18n.ts \
            lib \
            app \
            components \
            types \
            messages \
            backend \
            nginx \
            systemd \
            scripts \
            prerender-manifest.json
      
      - name: Deploy to server
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.PROD_SERVER_HOST }}
          username: ${{ secrets.PROD_SERVER_USER }}
          password: ${{ secrets.PROD_SERVER_PASSWORD }}
          port: 22
          source: "deploy.tar.gz"
          target: "/tmp"
      
      - name: Extract and deploy on server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.PROD_SERVER_HOST }}
          username: ${{ secrets.PROD_SERVER_USER }}
          password: ${{ secrets.PROD_SERVER_PASSWORD }}
          port: 22
          script: |
            set -e  # Прерываем только на критичных ошибках
            
            DEPLOY_DIR="/var/www/estenomada"
            BACKEND_DIR="$DEPLOY_DIR/backend"
            FRONTEND_DIR="$DEPLOY_DIR"
            
            # Функция для безопасного выполнения команд
            safe_run() {
              echo "▶️  Выполняю: $@"
              if "$@"; then
                echo "✅ Успешно: $@"
                return 0
              else
                echo "⚠️  Ошибка (продолжаем): $@"
                return 0  # Продолжаем даже при ошибке
              fi
            }
            
            echo "🚀 Начинаем деплой..."
            
            # Создаем бэкап текущей версии
            if [ -d "$DEPLOY_DIR" ]; then
              echo "📦 Создаю бэкап..."
              sudo tar -czf /tmp/backup-$(date +%Y%m%d-%H%M%S).tar.gz -C $DEPLOY_DIR . 2>/dev/null || true
            fi
            
            # Распаковываем новый код
            echo "📥 Распаковываем новый код..."
            sudo mkdir -p $DEPLOY_DIR
            cd /tmp
            if [ ! -f "deploy.tar.gz" ]; then
              echo "❌ Ошибка: deploy.tar.gz не найден!"
              exit 1
            fi
            echo "Размер архива: $(du -h deploy.tar.gz)"
            sudo tar -xzf deploy.tar.gz -C $DEPLOY_DIR
            if [ $? -ne 0 ]; then
              echo "❌ Ошибка при распаковке!"
              exit 1
            fi
            sudo rm deploy.tar.gz
            echo "✅ Код распакован"
            
            # Устанавливаем права
            echo "🔐 Устанавливаем права..."
            sudo chown -R www-data:www-data $DEPLOY_DIR
            sudo find $DEPLOY_DIR -type d -exec chmod 755 {} \;
            sudo find $DEPLOY_DIR -type f -exec chmod 644 {} \;
            sudo chmod +x $DEPLOY_DIR/scripts/*.sh 2>/dev/null || true
            
            # Backend: устанавливаем зависимости
            echo "📦 Устанавливаем Python зависимости..."
            cd $BACKEND_DIR
            
            # Проверяем Python (используем python3.12 который есть на сервере)
            PYTHON_CMD=$(which python3.12 || which python3 || which python)
            echo "Используем Python: $PYTHON_CMD"
            $PYTHON_CMD --version
            
            # Всегда пересоздаем venv для чистоты
            echo "Создаю виртуальное окружение..."
            sudo rm -rf venv
            sudo -u www-data $PYTHON_CMD -m venv venv
            
            # Исправляем права на исполняемые файлы в venv
            echo "Исправляю права в venv..."
            sudo chmod +x venv/bin/* 2>/dev/null || true
            sudo chown -R www-data:www-data venv
            
            # Проверяем что venv создан правильно
            if [ ! -f "venv/bin/python" ]; then
              echo "❌ Ошибка: venv не создан правильно"
              ls -la venv/bin/ || true
              exit 1
            fi
            
            # Устанавливаем зависимости используя python -m pip (надежнее)
            echo "Устанавливаю pip и зависимости..."
            if ! sudo -u www-data venv/bin/python -m pip install --upgrade pip; then
              echo "❌ Ошибка при обновлении pip"
              exit 1
            fi
            if ! sudo -u www-data venv/bin/python -m pip install --no-cache-dir -r requirements.txt; then
              echo "❌ Ошибка при установке зависимостей"
              exit 1
            fi
            echo "✅ Зависимости установлены"
            
            # Backend: создаем и применяем миграции
            echo "🗄️  Создаем новые миграции Django..."
            # Проверяем, что .env файл существует
            if [ ! -f ".env" ] && [ ! -f ".env.production" ]; then
              echo "⚠️  Предупреждение: .env файл не найден, миграции могут не работать"
            fi
            
            # makemigrations - не критично, если нет изменений
            set +e  # Временно отключаем прерывание для makemigrations
            MAKEMIGRATIONS_OUTPUT=$(sudo -u www-data venv/bin/python manage.py makemigrations 2>&1)
            MAKEMIGRATIONS_EXIT=$?
            set -e  # Включаем обратно
            
            if [ $MAKEMIGRATIONS_EXIT -eq 0 ]; then
              if echo "$MAKEMIGRATIONS_OUTPUT" | grep -q "No changes detected"; then
                echo "ℹ️  Нет новых миграций для создания"
              else
                echo "✅ Новые миграции созданы:"
                echo "$MAKEMIGRATIONS_OUTPUT" | head -20
              fi
            else
              echo "⚠️  Предупреждение при создании миграций (продолжаем):"
              echo "$MAKEMIGRATIONS_OUTPUT" | head -10
            fi
            
            echo "🗄️  Применяем миграции Django..."
            set +e  # Отключаем прерывание для проверки ошибок
            MIGRATE_OUTPUT=$(sudo -u www-data venv/bin/python manage.py migrate --noinput 2>&1)
            MIGRATE_EXIT=$?
            set -e  # Включаем обратно
            
            if [ $MIGRATE_EXIT -eq 0 ]; then
              echo "✅ Миграции применены успешно"
            else
              echo "❌ Ошибка при применении миграций!"
              echo "Вывод команды migrate:"
              echo "$MIGRATE_OUTPUT"
              echo ""
              echo "Проверяю статус миграций..."
              sudo -u www-data venv/bin/python manage.py showmigrations 2>&1 | head -30 || true
              echo ""
              echo "Проверяю настройки Django..."
              sudo -u www-data venv/bin/python manage.py check --deploy 2>&1 | head -20 || true
              exit 1
            fi
            
            # Backend: собираем статику
            echo "📦 Собираем статику Django..."
            sudo -u www-data venv/bin/python manage.py collectstatic --noinput || echo "⚠️  Ошибка collectstatic, но продолжаем"
            
            # Frontend: устанавливаем зависимости
            echo "📦 Устанавливаем Node.js зависимости..."
            cd $FRONTEND_DIR
            
            # Проверяем наличие .next директории
            if [ ! -d ".next" ]; then
              echo "❌ Ошибка: .next директория не найдена! Frontend не собран."
              exit 1
            fi
            
            # Очищаем и переустанавливаем зависимости (на случай проблем)
            echo "Очищаю старые зависимости..."
            sudo rm -rf node_modules package-lock.json 2>/dev/null || true
            
            # Устанавливаем зависимости (нужны все, не только production, для server.js)
            echo "Устанавливаю все зависимости..."
            if ! sudo -u www-data npm install; then
              echo "❌ Ошибка при установке зависимостей"
              echo "Пробую с правами root..."
              sudo npm install || exit 1
              sudo chown -R www-data:www-data node_modules package-lock.json 2>/dev/null || true
            fi
            
            # Проверяем что next установлен
            if [ ! -d "node_modules/next" ]; then
              echo "❌ Ошибка: модуль 'next' не установлен!"
              ls -la node_modules/ | head -10
              exit 1
            fi
            echo "✅ Зависимости установлены"
            
            # Очищаем кэш Python
            echo "🧹 Очищаем кэш Python..."
            sudo find $BACKEND_DIR -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true
            sudo find $BACKEND_DIR -name '*.pyc' -delete 2>/dev/null || true
            
            # Перезапускаем сервисы
            echo "🔄 Перезапускаем сервисы..."
            set +e  # Отключаем прерывание на ошибках для перезапуска
            sudo systemctl restart estenomada-backend
            BACKEND_RESTART=$?
            sudo systemctl restart estenomada-frontend
            FRONTEND_RESTART=$?
            set -e
            
            if [ $BACKEND_RESTART -eq 0 ]; then
              echo "✅ Backend перезапущен"
            else
              echo "⚠️  Backend не перезапущен (код: $BACKEND_RESTART)"
            fi
            
            if [ $FRONTEND_RESTART -eq 0 ]; then
              echo "✅ Frontend перезапущен"
            else
              echo "⚠️  Frontend не перезапущен (код: $FRONTEND_RESTART)"
            fi
            
            # Перезагружаем Nginx
            echo "🔄 Перезагружаем Nginx..."
            set +e
            sudo nginx -t
            NGINX_TEST=$?
            if [ $NGINX_TEST -eq 0 ]; then
              sudo systemctl reload nginx
              echo "✅ Nginx перезагружен"
            else
              echo "⚠️  Nginx конфигурация содержит ошибки"
            fi
            set -e
            
            # Проверяем статус сервисов
            echo "✅ Проверяем статус сервисов..."
            echo "--- Backend ---"
            sudo systemctl is-active estenomada-backend && echo "✅ Backend активен" || echo "⚠️  Backend не активен"
            echo "--- Frontend ---"
            sudo systemctl is-active estenomada-frontend && echo "✅ Frontend активен" || echo "⚠️  Frontend не активен"
            
            echo ""
            echo "✅ Деплой завершен!"
            echo "📊 Итоговый статус:"
            echo "   Backend: $(sudo systemctl is-active estenomada-backend)"
            echo "   Frontend: $(sudo systemctl is-active estenomada-frontend)"
      
      - name: Cleanup
        if: always()
        run: rm -f deploy.tar.gz
