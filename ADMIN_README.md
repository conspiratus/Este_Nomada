# Админ-панель Este Nómada

## 📋 Описание

Админ-панель для управления контентом сайта Este Nómada. Позволяет:
- Управлять историями/статьями блога
- Редактировать меню блюд
- Настраивать параметры сайта
- Настраивать интеграцию с Telegram
- Просматривать статистику

## 🔐 Доступ

**URL:** `/admin`

**Пароль по умолчанию:** `admin123`

⚠️ **ВАЖНО:** В продакшене обязательно измените пароль!

## 🚀 Использование

### Локальная разработка

1. Запустите dev-сервер:
```bash
npm run dev
```

2. Откройте http://localhost:3000/admin

3. Введите пароль: `admin123`

### В продакшене

**Текущая ситуация:** Админка создана, но для полноценной работы в статическом экспорте нужен backend.

**Варианты:**

1. **Локальная админка** (рекомендуется для начала):
   - Запускайте админку локально на своём компьютере
   - После редактирования контента пересобирайте сайт и загружайте на сервер

2. **Backend + админка на сервере**:
   - Настройте Node.js на сервере (см. `ACTIVATE_NODEJS.md`)
   - Уберите `output: 'export'` из `next.config.mjs`
   - Добавьте базу данных (например, SQLite или PostgreSQL)
   - Реализуйте API endpoints для сохранения данных

## 📁 Структура админки

```
/app/admin/
  ├── page.tsx              # Страница входа
  ├── dashboard/             # Главная панель
  │   └── page.tsx
  ├── stories/               # Управление историями
  │   └── page.tsx
  ├── menu/                  # Управление меню
  │   └── page.tsx
  ├── settings/              # Настройки сайта
  │   └── page.tsx
  ├── telegram/              # Настройка Telegram
  │   └── page.tsx
  └── stats/                 # Статистика
      └── page.tsx
```

## 🔧 Настройка

### Изменение пароля

Откройте `/app/admin/page.tsx` и измените:

```typescript
if (password === "admin123") {  // Замените на свой пароль
```

Или лучше вынесите в переменные окружения:

```typescript
if (password === process.env.NEXT_PUBLIC_ADMIN_PASSWORD) {
```

И добавьте в `.env.local`:
```
NEXT_PUBLIC_ADMIN_PASSWORD=ваш_безопасный_пароль
```

### Подключение backend

Для полноценной работы админки нужно:

1. **Создать API endpoints:**
   - `/api/admin/stories` - CRUD для историй
   - `/api/admin/menu` - CRUD для меню
   - `/api/admin/settings` - сохранение настроек
   - `/api/admin/telegram` - настройка Telegram

2. **Добавить базу данных:**
   - SQLite (просто)
   - PostgreSQL (для продакшена)
   - MongoDB (альтернатива)

3. **Реализовать авторизацию:**
   - JWT токены
   - Сессии
   - OAuth (опционально)

## 📝 TODO

- [ ] Реализовать сохранение данных через API
- [ ] Добавить загрузку изображений
- [ ] Реализовать редактор Markdown для историй
- [ ] Добавить предпросмотр изменений
- [ ] Реализовать логирование действий
- [ ] Добавить экспорт/импорт данных
- [ ] Улучшить безопасность (CSRF, rate limiting)

## 🔒 Безопасность

**Текущая реализация использует простую авторизацию через sessionStorage.**

Для продакшена обязательно:
1. Используйте серверную авторизацию
2. Храните пароли в хешированном виде
3. Используйте HTTPS
4. Добавьте rate limiting
5. Реализуйте CSRF защиту
6. Логируйте все действия администратора

## 📞 Поддержка

Если нужна помощь с настройкой админки или подключением backend - обращайтесь!



