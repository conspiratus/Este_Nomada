# Финальная настройка на сервере one.com

## ✅ Что уже сделано

1. ✅ Проект собран (`npm run build`)
2. ✅ Файлы загружены в веб-корень: `/customers/d/9/4/czjey8yl0/webroots/17a5d75c/`
3. ✅ Все необходимые файлы на месте:
   - `.next/` (production build)
   - `public/` (статические файлы)
   - `package.json` и `package-lock.json`
   - `server.js` (сервер для запуска)
   - `next.config.mjs`
   - `tsconfig.json`

## 🔧 Что нужно сделать в панели one.com

### 1. Активировать Node.js

1. Войдите в панель управления one.com
2. Перейдите в раздел **"Node.js"** или **"Applications"**
3. Найдите домен `estenomada.es`
4. Активируйте Node.js для этого домена
5. Выберите версию Node.js (рекомендуется 18.x или выше)
6. Укажите точку входа: `server.js`
7. Укажите команду запуска: `npm start` или `node server.js`
8. Укажите рабочую директорию: `/customers/d/9/4/czjey8yl0/webroots/17a5d75c`

### 2. Установить зависимости

После активации Node.js, подключитесь по SSH и выполните:

```bash
cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c
npm install --production
```

### 3. Настроить переменные окружения

Создайте файл `.env.production` в корне проекта:

```bash
cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c
cat > .env.production << EOF
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
EOF
```

### 4. Перезапустить приложение

В панели one.com перезапустите Node.js приложение или выполните:

```bash
pm2 restart server.js
# или
npm start
```

## 🌐 Проверка

После настройки откройте в браузере:
- https://estenomada.es
- https://www.estenomada.es

## 📝 Примечания

- Если Node.js не активируется через панель, обратитесь в поддержку one.com
- Убедитесь, что порт 3000 доступен (или измените в `server.js`)
- Проверьте логи приложения в панели one.com для диагностики проблем

## 🔄 Обновление проекта

Для обновления проекта в будущем:

1. Соберите проект локально: `npm run build`
2. Загрузите обновлённые файлы через SFTP/SCP
3. На сервере выполните: `npm install --production` (если обновились зависимости)
4. Перезапустите приложение



