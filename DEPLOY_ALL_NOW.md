# 🚀 Полный автоматический деплой на Production

## Одна команда для всего:

```bash
ssh -p 22 czjey8yl0_ssh@ssh.czjey8yl0.service.one "cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c && git pull origin feature/personal-cabinet-cart 2>/dev/null || echo 'Git pull skipped' && chmod +x scripts/deploy_all_to_prod.sh && ./scripts/deploy_all_to_prod.sh"
```

## Что делает скрипт:

✅ **Обновляет код** из бранча `feature/personal-cabinet-cart`  
✅ **Устанавливает зависимости** (geopy, markdown)  
✅ **Применяет миграции** базы данных  
✅ **Настраивает email** (info@nomadadeleste.com)  
✅ **Генерирует ENCRYPTION_KEY** (если нет)  
✅ **Создает настройки доставки** (если нет)  
✅ **Собирает статические файлы**  
✅ **Перезапускает сервисы**  
✅ **Отправляет тестовое письмо**  

## Выполнение по шагам:

```bash
# 1. Подключитесь к серверу
ssh -p 22 czjey8yl0_ssh@ssh.czjey8yl0.service.one

# 2. Перейдите в проект
cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c

# 3. Обновите код
git pull origin feature/personal-cabinet-cart

# 4. Запустите полный деплой
chmod +x scripts/deploy_all_to_prod.sh
./scripts/deploy_all_to_prod.sh
```

## После выполнения:

1. ✅ Проверьте почтовый ящик `info@nomadadeleste.com` - должно прийти письмо
2. ✅ Зайдите в админку: `/admin/`
3. ✅ Настройте доставку: `/admin/core/deliverysettings/`
   - Укажите координаты точки доставки
   - Настройте стоимость доставки
4. ✅ Проверьте страницу заказов: `/ru/order`

## Проверка работы:

```bash
# Проверьте email настройки
cat backend/.env | grep EMAIL

# Проверьте логи
tail -f backend/logs/django.log

# Проверьте статус сервиса
sudo systemctl status estenomada-backend
```

## Если что-то пошло не так:

### Откат изменений:

```bash
cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c
git checkout main
git pull origin main
```

### Восстановление .env:

```bash
cd backend
ls -la .env.backup.*
# Восстановите нужную версию
cp .env.backup.YYYYMMDD_HHMMSS .env
```

## Готово! 🎉

После выполнения скрипта все будет настроено и готово к работе:
- ✅ Личный кабинет
- ✅ Корзина
- ✅ Система заказов
- ✅ Email для подтверждений
- ✅ Расчет доставки

