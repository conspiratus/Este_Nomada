# Деплой Backend на one.com

## ✅ Что нужно сделать

### 1. Настроить Node.js на сервере

См. `ACTIVATE_NODEJS.md` - но теперь это обязательно, так как нужен backend.

### 2. Загрузить файлы проекта

```bash
# Локально соберите проект
npm run build

# Загрузите на сервер через SFTP/SCP:
# - .next/
# - public/
# - package.json
# - package-lock.json
# - next.config.mjs
# - server.js
# - tsconfig.json
# - lib/
# - app/
# - components/
# - middleware.ts
# - .env.local (с данными БД)
```

### 3. Установить зависимости на сервере

```bash
ssh -p 22 czjey8yl0_ssh@ssh.czjey8yl0.service.one
cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c
npm install --production
```

### 4. Инициализировать базу данных

См. `INIT_DB_ON_SERVER.md`

### 5. Создать администратора

```bash
npm run create-admin admin ваш_пароль
```

### 6. Запустить приложение

В панели one.com настройте Node.js приложение:
- Entry point: `server.js`
- Start command: `npm start` или `node server.js`
- Working directory: `/customers/d/9/4/czjey8yl0/webroots/17a5d75c`

## 🔧 Настройка переменных окружения

Создайте `.env.local` на сервере:

```env
DB_HOST=localhost
DB_USER=czjey8yl0_estenomada
DB_PASSWORD=Jovi4AndMay2020!
DB_NAME=czjey8yl0_estenomada
JWT_SECRET=случайная-строка-для-безопасности
NODE_ENV=production
NEXT_PUBLIC_BASE_URL=https://estenomada.es
```

## ⚠️ Важные замечания

1. **Безопасность:**
   - Измените `JWT_SECRET` на случайную строку
   - Используйте сильный пароль для администратора
   - Настройте HTTPS (обычно автоматически на one.com)

2. **Производительность:**
   - Next.js будет работать в режиме SSR
   - Страницы будут генерироваться на сервере
   - API routes будут работать динамически

3. **База данных:**
   - Убедитесь, что MySQL доступен с сервера
   - Проверьте права доступа пользователя БД
   - Регулярно делайте бэкапы БД

## 🐛 Отладка

Если что-то не работает:

1. Проверьте логи в панели one.com
2. Проверьте подключение к БД: `mysql -h localhost -u czjey8yl0_estenomada -p`
3. Проверьте, что Node.js запущен
4. Проверьте переменные окружения



