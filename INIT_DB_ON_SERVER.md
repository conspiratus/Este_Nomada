# Инициализация базы данных на сервере one.com

## Шаг 1: Подключиться к серверу по SSH

```bash
ssh -p 22 czjey8yl0_ssh@ssh.czjey8yl0.service.one
# Пароль: Drozdofil12345!
```

## Шаг 2: Перейти в директорию проекта

```bash
cd /customers/d/9/4/czjey8yl0/webroots/17a5d75c
```

## Шаг 3: Создать файл .env.local

```bash
cat > .env.local << 'EOF'
DB_HOST=localhost
DB_USER=czjey8yl0_estenomada
DB_PASSWORD=Jovi4AndMay2020!
DB_NAME=czjey8yl0_estenomada
JWT_SECRET=your-super-secret-jwt-key-change-in-production-$(openssl rand -hex 32)
NODE_ENV=production
NEXT_PUBLIC_BASE_URL=https://estenomada.es
EOF
```

**Важно:** 
- На one.com хост БД может быть `localhost` или специальный хост
- Проверьте в панели one.com правильный хост для MySQL
- Измените `JWT_SECRET` на случайную строку!

## Шаг 4: Инициализировать базу данных

### Вариант A: Через SQL скрипт напрямую

Подключитесь к MySQL через phpMyAdmin или командную строку и выполните SQL из файла `lib/db-init.sql`

### Вариант B: Через скрипт (если Node.js работает)

```bash
npm run init-db
```

## Шаг 5: Создать первого администратора

```bash
npm run create-admin admin ваш_безопасный_пароль
```

Или с дефолтными данными (не рекомендуется для продакшена):
```bash
npm run create-admin
# Создаст admin/admin123
```

## Шаг 6: Проверить подключение

Попробуйте зайти в админку:
- https://estenomada.es/admin
- Username: `admin`
- Password: (тот, что указали при создании)

## 🔍 Проверка подключения к БД

Если возникают проблемы, проверьте:

1. **Хост БД:**
   - В панели one.com найдите информацию о MySQL
   - Может быть `localhost`, `mysql.czjey8yl0.service.one` или IP

2. **Права доступа:**
   - Убедитесь, что пользователь `czjey8yl0_estenomada` имеет права на БД `czjey8yl0_estenomada`

3. **Порт:**
   - Обычно 3306, но может быть другой на shared hosting

## 📝 SQL для ручной инициализации

Если скрипт не работает, выполните SQL из `lib/db-init.sql` через phpMyAdmin:

1. Зайдите в phpMyAdmin
2. Выберите базу `czjey8yl0_estenomada`
3. Откройте вкладку "SQL"
4. Скопируйте содержимое `lib/db-init.sql`
5. Выполните запрос




