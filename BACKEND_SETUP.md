# Настройка Backend с MySQL

## ✅ Что реализовано

1. **Подключение к MySQL** - через `mysql2` с пулом соединений
2. **Система авторизации** - JWT токены + bcrypt для паролей
3. **API endpoints** для всех разделов админки
4. **Middleware** для защиты API routes
5. **Структура БД** - SQL миграции готовы

## 📋 Структура базы данных

Таблицы:
- `admins` - администраторы
- `stories` - истории/посты блога
- `menu_items` - блюда меню
- `settings` - настройки сайта
- `telegram_config` - конфигурация Telegram

## 🚀 Первоначальная настройка

### 1. Создать файл `.env.local`

```bash
cp .env.local.example .env.local
```

Заполните:
```
DB_HOST=localhost
DB_USER=czjey8yl0_estenomada
DB_PASSWORD=Jovi4AndMay2020!
DB_NAME=czjey8yl0_estenomada
JWT_SECRET=your-super-secret-jwt-key-change-in-production
NODE_ENV=development
```

**Важно:** На one.com хост БД может быть другим. Проверьте в панели управления.

### 2. Инициализировать базу данных

```bash
npm run init-db
```

Это создаст все таблицы и вставит начальные данные.

### 3. Создать первого администратора

```bash
npm run create-admin admin ваш_пароль
```

Или с дефолтными данными:
```bash
npm run create-admin
# Будет создан admin/admin123
```

### 4. Запустить dev-сервер

```bash
npm run dev
```

## 🔧 Настройка для one.com

### Вариант 1: Если БД доступна через localhost

На сервере one.com MySQL обычно доступен через `localhost`. Просто загрузите `.env.local` на сервер с правильными данными.

### Вариант 2: Если нужен специальный хост

Проверьте в панели one.com:
1. Зайдите в раздел "Databases" или "MySQL"
2. Найдите информацию о подключении
3. Обновите `DB_HOST` в `.env.local`

Возможные варианты:
- `localhost`
- `mysql.czjey8yl0.service.one`
- IP адрес сервера БД

## 📝 API Endpoints

### Авторизация
- `POST /api/admin/auth/login` - вход
- `POST /api/admin/auth/logout` - выход
- `GET /api/admin/auth/check` - проверка авторизации

### Истории
- `GET /api/admin/stories` - получить все
- `POST /api/admin/stories` - создать
- `GET /api/admin/stories/[id]` - получить одну
- `PUT /api/admin/stories/[id]` - обновить
- `DELETE /api/admin/stories/[id]` - удалить

### Меню
- `GET /api/admin/menu` - получить все
- `POST /api/admin/menu` - создать
- `GET /api/admin/menu/[id]` - получить одно
- `PUT /api/admin/menu/[id]` - обновить
- `DELETE /api/admin/menu/[id]` - удалить

### Настройки
- `GET /api/admin/settings` - получить все
- `PUT /api/admin/settings` - обновить

### Telegram
- `GET /api/admin/telegram` - получить конфигурацию
- `PUT /api/admin/telegram` - обновить конфигурацию
- `POST /api/admin/telegram` - запустить синхронизацию

## 🔒 Безопасность

- Все API routes админки защищены middleware
- Пароли хешируются через bcrypt
- JWT токены хранятся в httpOnly cookies
- В продакшене обязательно:
  - Изменить `JWT_SECRET` на случайную строку
  - Использовать HTTPS
  - Настроить CORS (если нужно)

## 🐛 Отладка

Если возникают проблемы с подключением к БД:

1. Проверьте доступность БД:
```bash
mysql -h localhost -u czjey8yl0_estenomada -p czjey8yl0_estenomada
```

2. Проверьте логи в консоли при запуске `npm run dev`

3. Убедитесь, что таблицы созданы:
```sql
SHOW TABLES;
```

## 📦 Деплой на сервер

После настройки Node.js на сервере:

1. Загрузите файлы проекта
2. Создайте `.env.local` на сервере с данными БД
3. Выполните `npm run init-db` на сервере
4. Создайте администратора: `npm run create-admin`
5. Запустите: `npm start` или `node server.js`



