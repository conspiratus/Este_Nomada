# 🚀 Автоматический деплой настроен!

## ✅ Как это работает:

**При каждом push в ветку `main` или `master` автоматически:**

1. ✅ GitHub Actions запускает пайплайн
2. ✅ Собирает фронтенд (Next.js)
3. ✅ Проверяет Django (`python manage.py check`)
4. ✅ Создает архив для деплоя
5. ✅ Загружает на сервер (`85.190.102.101`)
6. ✅ Распаковывает файлы
7. ✅ Устанавливает зависимости (Python и Node.js)
8. ✅ Применяет миграции Django (`python manage.py migrate`)
9. ✅ Собирает статику Django (`python manage.py collectstatic`)
10. ✅ Перезапускает сервисы (`estenomada-backend`, `estenomada-frontend`)
11. ✅ Перезагружает Nginx

## 📝 Как использовать:

### Автоматический деплой (рекомендуется):
```bash
git add .
git commit -m "Мои изменения"
git push origin main
```

**Всё!** GitHub Actions автоматически задеплоит на прод.

### Ручной запуск (если нужно):
1. Открой: https://github.com/conspiratus/Este_Nomada/actions
2. Выбери workflow **"Deploy to Production"**
3. Нажми **"Run workflow"**
4. Выбери ветку и нажми **"Run workflow"**

## 📊 Мониторинг:

- **Статус деплоя:** https://github.com/conspiratus/Este_Nomada/actions
- Логи доступны в реальном времени
- При ошибках увидишь в логах GitHub Actions

## ⚠️ Важно знать:

1. **Бэкап создается автоматически** перед каждым деплоем
2. **Миграции применяются автоматически** - не нужно запускать вручную
3. **Сервисы перезапускаются автоматически** - не нужно делать `systemctl restart`
4. **При ошибках** деплой может продолжиться (некритичные ошибки не останавливают процесс)

## 🔄 Что происходит на сервере:

```
1. Создается бэкап: /tmp/backup-YYYYMMDD-HHMMSS.tar.gz
2. Распаковывается новый код в /var/www/estenomada
3. Пересоздается venv (для чистоты)
4. Устанавливаются зависимости
5. Применяются миграции
6. Собирается статика
7. Перезапускаются сервисы
8. Перезагружается Nginx
```

## 🎯 Примеры использования:

### Обычный деплой:
```bash
git add .
git commit -m "Исправление бага"
git push origin main
# → Автоматический деплой запустится через ~30 секунд
```

### Деплой с тегами (версионирование):
```bash
git tag v1.0.0
git push origin main --tags
# → Также запустит деплой
```

## 🛡️ Безопасность:

- Все секреты хранятся в GitHub Secrets
- SSH подключение защищено паролем
- Бэкапы создаются перед каждым деплоем

---

**Теперь ты можешь просто делать `git push` и всё автоматически задеплоится! 🎉**

