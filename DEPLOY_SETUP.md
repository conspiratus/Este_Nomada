# 🚀 Настройка автоматического деплоя через GitHub Actions

## 📋 Что делает пайплайн

При каждом push в ветку `main` или `master` автоматически:
1. ✅ Собирает фронтенд (Next.js)
2. ✅ Проверяет Django (python manage.py check)
3. ✅ Создает архив для деплоя
4. ✅ Загружает на сервер
5. ✅ Распаковывает файлы
6. ✅ Устанавливает зависимости (Python и Node.js)
7. ✅ Применяет миграции Django
8. ✅ Собирает статику Django
9. ✅ Перезапускает сервисы (backend, frontend)
10. ✅ Перезагружает Nginx

## 🔐 Настройка GitHub Secrets

Нужно добавить секреты в настройках репозитория:

1. Перейди в **Settings** → **Secrets and variables** → **Actions**
2. Добавь следующие секреты:

### Обязательные секреты:

- **`PROD_SERVER_HOST`**: `85.190.102.101`
- **`PROD_SERVER_USER`**: `administrator`
- **`PROD_SERVER_PASSWORD`**: `Jovi4AndMay2020!`

## 📝 Как добавить секреты:

1. Открой: https://github.com/conspiratus/Este_Nomada/settings/secrets/actions
2. Нажми **"New repository secret"**
3. Добавь каждый секрет отдельно:
   - Name: `PROD_SERVER_HOST`, Value: `85.190.102.101`
   - Name: `PROD_SERVER_USER`, Value: `administrator`
   - Name: `PROD_SERVER_PASSWORD`, Value: `Jovi4AndMay2020!`

## 🎯 Использование

### Автоматический деплой:
Просто делай коммит и push в `main`:
```bash
git add .
git commit -m "Мои изменения"
git push origin main
```

GitHub Actions автоматически запустит деплой!

### Ручной запуск:
1. Перейди в **Actions** → **Deploy to Production**
2. Нажми **"Run workflow"**
3. Выбери ветку и нажми **"Run workflow"**

## 📊 Мониторинг

- Смотри статус деплоя в **Actions** вкладке GitHub
- Логи доступны в реальном времени
- При ошибках получишь уведомление

## ⚠️ Важно

- Пайплайн создает бэкап перед деплоем
- Миграции применяются автоматически
- Сервисы перезапускаются автоматически
- При ошибках деплой не прерывается полностью (продолжает работу)

## 🔄 Что происходит на сервере:

1. Создается бэкап текущей версии
2. Распаковывается новый код
3. Устанавливаются зависимости
4. Применяются миграции
5. Собирается статика
6. Перезапускаются сервисы
7. Перезагружается Nginx

Всё автоматически! 🎉

